# DSL Parser

A Python library for parsing Domain-Specific Languages (DSL).

---

## Description

**DSL Parser** is a lightweight and extensible library for building parsers for domain-specific languages.  
It supports triggers, cursor management, and returns data structures based on `BaseDataView`.

---

## Installation

```bash
pip install dslparser
